{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "King Of Nowhere — dev + Chrome",
            "url": "http://localhost:4321/fr/",
            "webRoot": "${workspaceFolder}/src",
            "preLaunchTask": "astro: dev"
        }
    ]
}
